- hosts: all
  tasks:
    - name: Gather facts
      setup:
      register: infoVM
        #filter: "*"
        #gather_subset: ["all"]
        #fact_path: /etc/ansible/facts.d
        #gather_timeout: 11
    - name: Print
      debug: 
        var: "{{ infoVM.stdout }}"
