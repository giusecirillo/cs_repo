- name: Playbook for Resize FS
  ansible.builtin.import_playbook: deployVMLinux.yml
  shell: cat "{{deploy.instance.hw_name}}.{{deploy.instance.hw_product_uuid}}"
  register: host
  hosts: "{{host}}"
  gather_facts: no
  remote_user: ansible
  #become_method: sudo
  become: True
  tasks:
  - name: Resize FS 
    ansible.builtin.command:
      cmd: /bin/sh /home/ansible/bin/resize_fs.sh
    register: fs_resize
