- name: Playbook for Resize FS
  hosts: "{{config.name}}"
  gather_facts: no
  remote_user: ansible
  #become_method: sudo
  become: True
  tasks:
  - name: Resize FS 
    ansible.builtin.command:
      cmd: /bin/sh /home/ansible/bin/resize_fs.sh
    register: fs_resize
