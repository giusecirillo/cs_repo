---
- hosts: all
  tasks:
    - name: Update repository cache
      ansible.builtin.apt:
        update_cache: yes
        cache_valid_time: 3600
      when: ansible_distribution == "Ubuntu"
      tags:
        - update_repo
